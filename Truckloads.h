#pragma once
class Truckloads
{
public:
	int numTrucks(int numCrates, int loadSize);
	int numTrucks(float numCrates, int loadSize);
	int numTrucks(float numCrates, float loadSize);
	int numTrucks(int numCrates, float loadSize);
};